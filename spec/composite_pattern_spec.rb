require 'spec_helper'

describe CompositePattern do
  it 'has a version number' do
    expect(CompositePattern::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
